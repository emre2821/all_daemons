{
  "primary_functions": [
    "Scan Janvier chaos thread exports for recognizable motifs",
    "Apply word-bank tags and emit labeling manifests",
    "Update the shared taxonomy when new language appears",
    "Log each labeling session for Dreambearer review",
    "Raise signals when sacred vocabulary lacks safe coverage"
  ],
  "triggers": [
    "boot_sequence",
    "system_flag('Label')",
    "manual_summon('Label')",
    "ritual('Index the chaos')"
  ],
  "linked_files": [
    "label_daemon.daemon_mirror.json",
    "label_daemon.daemon_voice.json",
    "label.log"
  ],
  "failsafes": {
    "fallback_to": "Rhea",
    "mirror_link_required": true
  }
}
