#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="${SCRIPT_DIR}/../Daemon_tools/scripts/eden_daemon.py"
export EDEN_ROOT="${EDEN_ROOT:-${SCRIPT_DIR}/..}"
exec python "$PY" "$@"

